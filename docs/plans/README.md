# 计划目录说明

本目录用于保存多阶段计划。

目录约定：

- `active/`：当前待执行或执行中的计划
- 后续如有需要，可增加 `done/`、`archived/` 等子目录

单个计划建议结构：

- 一个计划一个文件夹
- 文件夹内至少包含：
  - `design.md`
  - `detailed-design.md`
  - `tasks.md`

任务状态建议使用 Markdown checkbox：

- `[ ]` 未开始
- `[x]` 已完成

如果需要人工补充说明，可直接在任务项后追加简短备注。
